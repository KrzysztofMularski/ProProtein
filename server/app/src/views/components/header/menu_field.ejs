<% const isSelected = title == selected %>

<a href="/<%= link %> " class="flex flex-row items-center gap-16 px-8 py-5 text-gray-100 hover:text-gray-700 hover:bg-gray-300  <%= isSelected ? 'bg-gray-500' : '' %>">
    <div class="w-6">
        <%- include (`../../../../public/icons/${icon}`) %>
    </div>
    <p class="font-quicksand font-light text-base"><%= title %></p>
</a>